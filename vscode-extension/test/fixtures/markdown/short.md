# Note rapide

Ceci est un document court, sans frontmatter ni bloc de code, utile pour
tester le cas le plus simple du parseur Markdown.

Une seule phrase suffit pour vérifier la segmentation de base.
